; DIYAR PHP-FPM pool — LARGE VPS profile (Phase 28.14)
; Assumptions: 8 GB RAM VPS, 3 GB MySQL, 1 GB Redis, 8 queue workers
; Available for PHP: ~2.5 GB → ~35–40 workers @ ~64 MB each

[diyar]
user = www-data
group = www-data
listen = /run/php/php8.2-fpm-diyar.sock
listen.owner = www-data
listen.group = www-data
listen.mode = 0660

pm = dynamic
pm.max_children = 36
pm.start_servers = 8
pm.min_spare_servers = 6
pm.max_spare_servers = 12
pm.max_requests = 1000

request_terminate_timeout = 120s
request_slowlog_timeout = 10s
slowlog = /var/log/php8.2-fpm-diyar-slow.log

php_admin_value[memory_limit] = 256M
php_admin_value[upload_max_filesize] = 12M
php_admin_value[post_max_size] = 14M
php_admin_value[max_execution_time] = 120

catch_workers_output = yes
clear_env = no
