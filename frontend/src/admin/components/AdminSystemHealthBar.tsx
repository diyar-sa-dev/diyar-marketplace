import { AdminPlatformHealthPanel } from './AdminPlatformHealthPanel.tsx';

/** Compact system health strip for the admin sidebar. */
export function AdminSystemHealthBar() {
  return <AdminPlatformHealthPanel variant="sidebar" />;
}
