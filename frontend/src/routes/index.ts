/**
 * Route definitions remain in App.tsx during Stage 1.
 * This module is the future home for extracted route configuration.
 */
export const ROUTE_PREFIX = {
  dashboard: '/dashboard',
  auth: '/auth',
} as const;
