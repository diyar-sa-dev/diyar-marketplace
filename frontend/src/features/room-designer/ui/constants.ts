/** Tailwind default `lg` — matches project utility usage (e.g. MobileBottomNav md:hidden). */
export const ROOM_DESIGNER_LG_BREAKPOINT_PX = 1024;

/** Minimum comfortable touch target (WCAG 2.5.5 advisory). */
export const ROOM_DESIGNER_TOUCH_TARGET_PX = 44;

export const ROOM_DESIGNER_CANVAS_MIN_HEIGHT_PX = 220;
