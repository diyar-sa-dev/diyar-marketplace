# Phase 5.5.2 — Catalog API

**Status:** COMPLETE

## Plan

Extend backend to support storefront filter/sort/vendor directory needs.

## Tasks

- [x] `ProductService::applyFilters()` extensions
- [x] `VendorService` + `GET /vendors`
- [x] `CategoryService` type filter
- [x] `ProductCardResource` / `VendorCardResource` enrichment
- [x] `ProductFilterTest`
