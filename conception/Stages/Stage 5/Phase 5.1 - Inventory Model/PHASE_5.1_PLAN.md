# Phase 5.1 — Inventory Model Plan

## Objective
Reliable inventory domain with enforced `available = stock - reserved`.

## Tasks
- [x] Audit existing schema (one row per product)
- [x] `ProductInventory::computeAvailableQuantity()` / `syncAvailableQuantity()`
- [x] `assertInvariants()` guard
- [x] `ProductInventoryFactory`
- [x] Tests: `InventoryModelTest`

## Completion
See `PHASE_5.1_COMPLETION_REPORT.md`
